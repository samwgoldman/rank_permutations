// Creates the identity permutation of length n
int* create_identity_permutation(int n);

// Creates a random permutation of length n
int* create_random_permutation(int n);

// Creates and returns the inverse of the permutation p of length n
int* inverse_permutation(int n, int *p);

// Prints the permutation n of length n
void print_permutation(int n, int *p);

int factorial(int n);
void swap(int* a, int* b);